# Runtime dependencies only. Direct dependencies are pinned; pip resolves the
# rest. Test tooling lives in requirements-dev.txt so it never reaches the
# production image.

fastapi==0.141.1
uvicorn[standard]==0.52.1
pydantic-settings==2.15.0
sqlalchemy==2.0.51
psycopg[binary]==3.3.4
pgvector==0.5.0
alembic==1.19.1
pypdf==6.15.0
fastembed==0.8.0
python-multipart==0.0.32
python-dotenv==1.2.2
httpx==0.28.1
pyjwt==2.13.0
argon2-cffi==25.1.0
email-validator==2.3.0
